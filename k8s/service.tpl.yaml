apiVersion: v1
kind: Service
metadata:
  name: ${SERVICE_NAME}
spec:
  selector:
    app: ${SERVICE_NAME}
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: ${NODE_PORT}
  type: NodePort
